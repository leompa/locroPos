// GitHub Pages/static hosting Firebase environment configuration.
// Replace these values with your Firebase web app configuration before deploy.
// For local Express development, values from .env override this file at /js/config/env.js.
export const firebaseConfig = Object.freeze({
  apiKey: 'your-api-key',
  authDomain: 'your-project.firebaseapp.com',
  projectId: 'your-project-id',
  storageBucket: 'your-project.appspot.com',
  messagingSenderId: 'your-sender-id',
  appId: 'your-app-id'
});
