// Firebase web configuration for static hosting (GitHub Pages included).
// These values are public by design in Firebase web apps. Replace them with
// your Firebase project settings before deploying.
export const firebaseConfig = Object.freeze({
  apiKey: 'your-api-key',
  authDomain: 'your-project.firebaseapp.com',
  projectId: 'your-project-id',
  storageBucket: 'your-project.appspot.com',
  messagingSenderId: 'your-sender-id',
  appId: 'your-app-id'
});
